apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/bygui86/spring-swagger:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/bygui86/spring-swagger:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: spring-swagger
        chartPath: charts/spring-swagger
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/bygui86/spring-swagger"
          image.tag: "{{.TAG}}"
